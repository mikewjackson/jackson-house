{
  "name": "jackson-house",
  "private": true,
  "scripts": {
    "build": "python .\\scripts\\build.py",
    "serve": "python -m http.server --directory dist 8000",
    "lint:css": "stylelint \"static/**/*.css\" --config stylelint.config.mjs",
    "test:playwright": "playwright test"
  },
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^7.0.2",
    "@playwright/test": "^1.59.1",
    "playwright": "^1.40.0",
    "stylelint": "^16.26.1",
    "stylelint-config-standard": "^39.0.1"
  }
}
